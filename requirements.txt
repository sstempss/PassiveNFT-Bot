# Оптимизированные зависимости для быстрого деплоя PassiveNFT Bot
# Убраны пакеты, требующие компиляции C расширений

# Telegram Bot Framework (готовая сборка)
python-telegram-bot>=22.0

# Асинхронная база данных (готовая сборка)  
aiosqlite>=0.19.0

# HTTP сервер для webhook (готовая сборка)
aiohttp>=3.9.0

# Утилиты окружения (без компиляции)
python-dotenv>=1.0.0

# Дата и время (без компиляции)
python-dateutil>=2.8.0

# Валидация данных (без компиляции)
pydantic>=2.0.0

# HTTP клиент (готовая сборка)
httpx>=0.25.0

# Минимальные зависимости для быстрой установки
# Убраны: numpy, cryptography, orjson, asyncio-mqtt - требуют компиляции
