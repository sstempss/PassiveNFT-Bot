[variables]
PYTHON_VERSION = "3.14"

[phases.build]
cmds = [
    "export PIP_DEFAULT_TIMEOUT=100",
    "export PIP_DISABLE_PIP_VERSION_CHECK=1", 
    "pip install --no-cache-dir --prefer-binary -r requirements.txt"
]

[phases.start]
cmd = "python bot_deploy_FIXED_FINAL.py"
